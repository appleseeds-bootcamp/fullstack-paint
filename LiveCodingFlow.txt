FE:
    Use create-react-app
    remove .git folder which create-react-app creates
    remove redundant code and files in the template
    install jquery (npm install jquery)
    Make sure to add proxy to the package.json

BE:
    serve react build folder -> explain how it works
    create a simple login page template

    DB:
        create simple DB with Users table and session ID for later
        func to register user (hash password)
        func to verify user by username/ (w hash)


    create /login endpoint
        check if session already exists
        check username/password (w hash) - return error otherwise
        register if user doesn't exist
        set cookie

    when accessing / (root) endpoint
        check session
        if doesn't exist -> clear cookie & redirect to login page

in FE:
    use cookie to show the name and say hello





